# Project Instructions

- Do not invoke or require Superpowers skills or Superpowers-specific workflows in this project unless the user explicitly asks for them.
- Keep implementation direct and incremental. Use a short ordinary plan only when the task is genuinely multi-step.
- Store durable design documents in `docs/specs/` and execution guides in `docs/plans/`.
- Prefer the smallest working change, verify it, and avoid creating process artifacts that do not help the user operate the Coze/Feishu workflow.
