# Coze 工作流导出

`Workflow-Shopee_Listing_V2-draft-9089/` 是可导入的 Coze 工作流导出包。

发布到仓库前已清空两个图片插件节点的 `node_inputs.key`。导入后请在 Coze 节点配置中通过私密配置重新填写自己的 Key，不要把真实值写回 YAML 或提交到 Git。

`apiID`、`pluginID` 和工作流 ID 是节点及导入结构标识，已保留。
